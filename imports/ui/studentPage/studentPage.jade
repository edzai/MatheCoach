template(name="studentPage")
  with student
    +studentPageDisplay
    
template(name="studentPageDisplay")
  .ui.text.container
    h1.header #{name}
    +activityGraph(userId=_id)
    .ui.list
      each submissions
        +submissionDisplay
